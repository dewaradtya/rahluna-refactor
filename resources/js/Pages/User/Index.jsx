import Table from '../../Components/Table';
import MainLayout from '../../Layouts/MainLayout';

const Index = () => {
    return <div>Semua User</div>;
};

Index.layout = (page) => <MainLayout children={page} title="User Page" />;

export default Index;
